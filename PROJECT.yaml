# ============================================================================
# PROJECT.yaml - Archivo de metadatos del proyecto
# ============================================================================
#
# PARA CLAUDE (IA): Este archivo es leido por el Project Manager (99999).
#
# INSTRUCCIONES:
#   - Si cambias de version, actualiza 'project.version'
#   - Siempre actualiza 'updated_at' y 'updated_by' al modificar
#   - 'updated_by' debe ser tu identificador (ej: claude-101)
#
# NO MODIFICAR SIN RAZON:
#   - project.code (identificador unico)
#   - server.base_path
#
# NOTA: repo publico. base_path relativo a proposito - las rutas reales de esta
# maquina viven en PRIVATE-NOTES.md (gitignored). Mismo criterio que 077 y 091.
# ============================================================================

project:
  code: web26-101
  name: YCloud MCP
  version: 0.1.0
  status: development
  description: Servidor MCP para la API v2 de YCloud - WhatsApp Business, SMS y email
  type: tool

server:
  location: local-windows
  base_path: .

service:
  type: none
  expect_offline: true
  notes: "servidor MCP stdio - no escucha en un puerto (estrategia de webhooks sin decidir)"

stack:
  primary: python
  framework: mcp
  components:
    - MCP stdio
    - API v2 de YCloud

urls:
  repository: https://github.com/abelardodiaz/web26-101-ycloud-mcp
  repository_mirror: https://gitlab.com/abelardodiaz/web26-101-ycloud-mcp

related:
  - code: web26-851
    relation: origen
    note: "el cliente de la API v2 se extrae de 851-opencode-ycloud (privado, vivo)"

notes: >
  Andamiaje preparado por claude-996 el 2026-08-14. Sin codigo propio todavia; el
  punto de partida es extraer el cliente de API de web26-851. Ver docs/101-ycloud-mcp/
  en web25-996.

updated_at: "2026-08-14T00:00:00"
updated_by: claude-996
